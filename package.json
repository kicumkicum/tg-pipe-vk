{
  "name": "tg-pipe-vk",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "tg:set-webhook": "bash --noprofile --norc -c 'set -euo pipefail; : \"${TG_TOKEN:?TG_TOKEN is required}\"; : \"${TG_WEBHOOK_URL:?TG_WEBHOOK_URL is required}\"; curl -sS \"https://api.telegram.org/bot${TG_TOKEN}/setWebhook\" --data-urlencode \"url=${TG_WEBHOOK_URL}\" --data-urlencode \"allowed_updates=[\\\"message\\\"]\" ${TG_WEBHOOK_SECRET:+--data-urlencode \"secret_token=${TG_WEBHOOK_SECRET}\"}; echo; curl -sS \"https://api.telegram.org/bot${TG_TOKEN}/getWebhookInfo\"; echo'",
    "tg:webhook-info": "bash --noprofile --norc -c 'set -euo pipefail; : \"${TG_TOKEN:?TG_TOKEN is required}\"; curl -sS \"https://api.telegram.org/bot${TG_TOKEN}/getWebhookInfo\"; echo'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2",
    "vercel": "^51.4.0"
  },
  "dependencies": {
    "@vercel/node": "^5.7.7"
  }
}
